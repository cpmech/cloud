# Custom Resource Lambdas (CRL) for CDK projects

az-cdk-crl (AmaZon CDK Custom Resource Lambdas) implements the lambda functions for the az-cdk package.

## Installation

```bash
yarn add @cpmech/az-cdk-crl
```
